import { ProductcardHoverDirective } from './productcard-hover.directive';

describe('ProductcardHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductcardHoverDirective();
    expect(directive).toBeTruthy();
  });
});
